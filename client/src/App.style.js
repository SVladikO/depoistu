import styled from 'styled-components'
import {COLOR} from "./utils/theme";

export const Wrapper = styled.div`
  height: 100vh;
  margin: 0 auto;
`;

document.body.style.backgroundColor = COLOR.ACCENT2;