import styled from "styled-components";
import {COLOR} from "../../theme";

export const Wrapper = styled.div`
  font-style: normal;
  font-weight: 700;
  font-size: 16px;
  line-height: 19px;
  color: ${COLOR.PRIMARY};
`