import React from 'react';
import {useScrollUp} from "../../utils/hook";

const AboutUsPage = () => {
    useScrollUp();

    return (
        <div>
            We are the best.
        </div>
    );
};

export default AboutUsPage;